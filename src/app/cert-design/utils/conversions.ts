// Utility functions for Certificate Designer

export const mmToPx = (mm: number, scale: number) => mm * 3.7795275591 * scale;
export const pxToMm = (px: number, scale: number) => px * 0.264583 / scale;
